{
  "name": "reizeslaw-blog-tooling",
  "version": "1.0.0",
  "private": true,
  "description": "Build tooling for the Reizes Law blog: renders content/blog/*.md into static HTML using the site template, and reconciles the blog index + sitemap.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build_blog.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^12.0.0"
  }
}
